---

- name: create includes.d/limits.rules
  when:
    - not running_in_check_mode
  ansible.builtin.template:
    src: includes.d/limits.rules.j2
    dest: /etc/nginx/includes.d/limits.rules
    owner: root
    group: root
    mode: "0644"
  notify:
    - validate nginx config
    - reload nginx

...
