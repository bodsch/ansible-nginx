---

nginx_dependencies:
  - ssl-cert
  - python3-passlib

nginx_certbot_packages: []

nginx_packages:
  - nginx

nginx_user: www-data
nginx_group: www-data

nginx_mime_file_path: /etc/nginx/mime.types

...
